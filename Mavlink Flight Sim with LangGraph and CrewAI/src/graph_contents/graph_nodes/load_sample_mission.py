from graph_contents.missionState import MissionState

#load sample mission is currently done through the human_mission_input node
#right now i don't it's necessary to change

#not used currently. I think since it's already a node
#it would be simple to make this a tool that gives a standard planning
#process to make small asks into a SOP approach to an operaiton

#another idea is make this a tool to take human input and perform some prompt engineering
def load_sample_mission(state: MissionState) -> MissionState:
    # TODO: Implement logic to load a sample mission
    return state

